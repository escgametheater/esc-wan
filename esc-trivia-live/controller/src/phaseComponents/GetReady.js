import React from "react";
import { Container } from "reactstrap";

import "./GetReady.css";

const GetReady = (props) => (
	<div className="getready-phase">
		<Container>
			<h1>Get ready, the next round is about to begin...</h1>
		</Container>
	</div>
);

export default GetReady;